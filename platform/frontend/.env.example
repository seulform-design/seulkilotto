# platform/frontend/.env.example
# 게이트웨이 경유가 기본. 빈 문자열 = 상대경로 호출 (= 동일 origin).
# 외부 origin (예: native 모바일 클라이언트)에서 백엔드를 직접 호출할 때만 절대 URL을 지정.
VITE_API_BASE=

# 빌드 시 표시할 환경 라벨 (UI 우상단 등 디버깅용). 운영에서는 빈 값.
VITE_ENV_LABEL=
